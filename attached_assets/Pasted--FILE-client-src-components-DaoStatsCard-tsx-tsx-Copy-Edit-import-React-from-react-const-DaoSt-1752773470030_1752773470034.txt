✅ FILE: client/src/components/DaoStatsCard.tsx
tsx
Copy
Edit
import React from 'react';

const DaoStatsCard = () => {
  const stats = {
    xp: 12345,
    rank: 42,
    successRate: '92%',
    votingWeight: '1,500 GTT',
  };

  return (
    <div className="grid">
      <article style={{ background: 'linear-gradient(to right, #6a11cb, #2575fc)', color: '#fff' }}>
        <hgroup>
          <h2>🏆 DAO Reputation</h2>
          <h3>Performance Overview</h3>
        </hgroup>
        <ul>
          <li><strong>Total XP:</strong> {stats.xp}</li>
          <li><strong>Global Rank:</strong> #{stats.rank}</li>
          <li><strong>Success Rate:</strong> {stats.successRate}</li>
          <li><strong>Voting Power:</strong> {stats.votingWeight}</li>
        </ul>
      </article>
    </div>
  );
};

export default DaoStatsCard;
✅ FILE: client/src/components/SealBadge.tsx
tsx
Copy
Edit
import React from 'react';

interface Props {
  type: 'Standard' | 'Premium' | 'Legal' | 'Diamond';
}

const badgeColor = {
  Standard: '#B0BEC5',
  Premium: '#00C853',
  Legal: '#2962FF',
  Diamond: '#AA00FF'
};

export default function SealBadge({ type }: Props) {
  return (
    <span
      style={{
        backgroundColor: badgeColor[type],
        padding: '0.25em 0.75em',
        borderRadius: '999px',
        color: 'white',
        fontWeight: 'bold',
        fontSize: '0.9em',
        display: 'inline-block',
        marginRight: '0.5em'
      }}
    >
      {type} Seal
    </span>
  );
}
✅ UPDATED FILE: client/src/pages/insight.tsx
Here’s the final polished Insight page with full component integration and branding:

tsx
Copy
Edit
import React from 'react';
import DaoXpGraph from '../components/DaoXpGraph';
import DaoStatsCard from '../components/DaoStatsCard';
import CapsuleTimeline from '../components/CapsuleTimeline';
import SealStudio from '../components/SealStudio';

export default function Insight() {
  return (
    <main className="container">
      <hgroup>
        <h1>🧠 Insights & Reputation Center</h1>
        <h3>Track DAO performance, capsule evolution, and Veritas sealing</h3>
      </hgroup>

      <DaoStatsCard />
      <hr />

      <DaoXpGraph />
      <hr />

      <CapsuleTimeline />
      <hr />

      <SealStudio />
    </main>
  );
}
✅ NAVIGATION UPDATE (if not already added):
tsx
Copy
Edit
<ul>
  <li><a href="/insight">Insights</a></li>
</ul>
🔁 REPLIT INSTRUCTIONS
From Replit shell:

bash
Copy
Edit
cd client
npx tsc --noEmit
npm run dev
Then open in browser at:

arduino
Copy
Edit
https://your-repl-name.replit.app/insight
🛡️ If you're seeing component issues, clear your browser cache and ensure file paths match your component structure exactly.