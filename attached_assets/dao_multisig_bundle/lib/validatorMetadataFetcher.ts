export async function fetchValidatorMetadata(wallet) {
  // Simulate fetching from a decentralized registry or blockchain
  const metadata = {
    name: "NodeAlpha",
    totalCapsules: 108,
    totalVotes: 500,
    reputation: 92,
    active: true,
  };
  return metadata;
}
