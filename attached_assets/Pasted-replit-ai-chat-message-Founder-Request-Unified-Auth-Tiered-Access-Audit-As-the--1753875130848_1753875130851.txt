replit ai chat --message "
🚀 **Founder Request: Unified Auth & Tiered Access Audit** 🚀

As the founder and creator of **GuardianChain**, I need you to:

1. **Discover & Catalog**  
   • List every file and folder involved in authentication, onboarding, login, user profile, dashboard, tiered-access, enterpriseAuth, masterAdmin, and footer/navigation links.  

2. **Eliminate Public Exposure**  
   • Identify all routes, nav items or footer links that expose `enterpriseAuth` or `masterAdmin` to unauthenticated or free users.  
   • Remove or hide these so there is **only one** public entry point: `/login`.  

3. **Single Entry Point**  
   • Refactor the app so **all** users—free, paid tiers, enterprise, and master admins—must pass through the same `/login` route.  
   • After login, redirect based on role/subscription status:
     - **Free** → `/dashboard/free`
     - **Paid Tier X** → `/dashboard/tierX`
     - **Enterprise** → `/dashboard/enterprise`
     - **Master Admin** → `/dashboard/admin`

4. **Role-Based Nav & Features**  
   • Implement or tighten middleware/HOC on every protected route so that only users with the proper role/subscription see the matching nav items and pages.  
   • Verify that free users **cannot** see or access paid features.  
   • Sync feature visibility dynamically against the user’s subscription record (e.g., Stripe customer metadata).

5. **Subscription & Billing Integration**  
   • Audit or add hooks to your signup flow to collect subscription fees via Stripe (or your chosen provider).  
   • Ensure subscription status is stored in your user database and used by your front‐end to gate feature flags.

6. **Remove Duplication & Sync Auth Logic**  
   • Delete any duplicate or conflicting auth code—e.g. multiple login forms, multiple middleware implementations.  
   • Consolidate all auth/session logic into a single module (e.g. `auth.js` or a Next.js middleware).

7. **Deliverables**  
   • A detailed list of file renames/deletions.  
   • Exact code diffs for each modified file (React components, API routes, middleware).  
   • Configuration changes (next.config.js, replit.nix, .replit, environment variables).  
   • A summary of tests or manual steps to verify that:
     - There is only one `/login`.  
     - Unauthorized access is blocked.  
     - Tiered features are correctly hidden/shown.  
     - Subscription fees are collected on signup.

Please produce a **complete audit report** and **patch-ready diffs** so I can apply these changes immediately.  
"
