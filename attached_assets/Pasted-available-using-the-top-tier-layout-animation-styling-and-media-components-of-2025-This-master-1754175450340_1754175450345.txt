available, using the top-tier layout, animation, styling, and media components of 2025.

This master command not only fixes layout and rendering, but elevates GuardianChain visually beyond most dApps — with a polished, animated UI, smooth layout flow, responsive visuals, and dynamic logo integration.

🧠 REPLIT AI COMMAND: GUARDIAN-VISUAL-SUPREMACY-UPGRADE-V4
markdown
Copy
Edit
🛠 COMMAND: GUARDIAN-VISUAL-SUPREMACY-UPGRADE-V4

🎯 GOAL: Fully upgrade GuardianChain’s frontend to 2025 visual standards using the most powerful open-source UI primitives, layout engine, motion components, and image loaders.

---

✅ CORE VISUAL STACK (to install or verify):
- TailwindCSS 3.4+
- ShadCN/UI (https://ui.shadcn.com/docs/installation)
- Framer Motion (animations)
- Lucide React (icons)
- Next Font (for optimized font loading)
- Next/Image (for responsive logos)
- Radix UI Primitives (modal, dialog, accordion)
- Tailwind Typography plugin (if needed)

---

✅ STEP 1: HERO + HOMEPAGE REPLACEMENT

📁 `/app/page.tsx` or `/pages/index.tsx`:

```tsx
import { Button } from "@/components/ui/button";
import Image from "next/image";
import { motion } from "framer-motion";

export default function Home() {
  return (
    <main className="min-h-screen flex flex-col justify-between items-center bg-gradient-to-b from-white via-yellow-50 to-yellow-200 px-6 py-12">
      <section className="text-center">
        <motion.h1
          initial={{ opacity: 0, y: -20 }}
          animate={{ opacity: 1, y: 0 }}
          transition={{ duration: 0.8 }}
          className="text-5xl font-extrabold text-yellow-700 drop-shadow-md"
        >
          🛡️ GuardianChain
        </motion.h1>

        <p className="text-lg mt-4 text-gray-700 max-w-xl mx-auto">
          Immutable memory. Verifiable truth. Capsule-based sovereign storage and yield.
        </p>

        <div className="mt-8 flex justify-center gap-4">
          <Button variant="default" className="bg-yellow-500 hover:bg-yellow-600 text-white">
            Launch Dashboard
          </Button>
          <Button variant="outline" className="border-yellow-600 text-yellow-700">
            ▶️ Watch Demo
          </Button>
        </div>
      </section>

      <section className="mt-10 flex flex-col items-center">
        <Image
          src="/assets/gtt-logo.png"
          alt="GTT Logo"
          width={200}
          height={200}
          className="object-contain drop-shadow-xl"
        />
        <p className="text-xs text-gray-400 mt-2">© 2025 GuardianChain</p>
      </section>
    </main>
  );
}
✅ STEP 2: MODERN NAVBAR + FOOTER USING SHADCN + LUCIDE

📁 /components/ui/navbar.tsx

tsx
Copy
Edit
import { Home, Vault, Wallet, ShieldCheck } from "lucide-react";
import Link from "next/link";

export function Navbar() {
  return (
    <nav className="w-full px-6 py-4 flex justify-between items-center bg-white border-b shadow-sm fixed top-0 z-50">
      <Link href="/" className="text-xl font-bold text-yellow-700 flex items-center gap-2">
        <ShieldCheck className="text-yellow-500" /> GuardianChain
      </Link>

      <div className="flex gap-6 text-sm font-medium text-gray-700">
        <Link href="/dashboard">Dashboard</Link>
        <Link href="/vault">Vault</Link>
        <Link href="/mint">Mint</Link>
        <Link href="/verify">Verify</Link>
      </div>
    </nav>
  );
}
📁 /components/ui/footer.tsx

tsx
Copy
Edit
export function Footer() {
  return (
    <footer className="w-full py-6 text-center text-sm text-gray-500 mt-20">
      <p>© 2025 GuardianChain. All rights reserved.</p>
    </footer>
  );
}
✅ STEP 3: OPTIMIZED FONT SYSTEM

📁 /app/layout.tsx (Next 13+) or _app.tsx (Next 12)

tsx
Copy
Edit
import { Inter } from "next/font/google";
import "@/styles/globals.css";

const inter = Inter({ subsets: ["latin"] });

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body className={inter.className + " bg-white text-gray-900"}>
        {children}
      </body>
    </html>
  );
}
✅ STEP 4: IMAGE + LOGO FIX

📁 Place all logos into /public/assets/

Examples:

guardian-logo.png

gtt-logo.png

veritas-badge.svg

Use with next/image for full optimization:

tsx
Copy
Edit
<Image src="/assets/gtt-logo.png" width={200} height={200} alt="Guardian Truth Token" />
✅ STEP 5: INSTALL DEPENDENCIES

If missing, install:

bash
Copy
Edit
npm install tailwindcss framer-motion lucide-react classnames @radix-ui/react-slot
npx shadcn-ui@latest init
✅ STEP 6: STYLED CTA, ICON BUTTONS, AND LIVE HERO

Use:

tsx
Copy
Edit
<Button variant="ghost"><RocketIcon className="mr-2" /> Explore Capsules</Button>
Or icon-only:

tsx
Copy
Edit
<Button size="icon"><UploadCloud className="text-yellow-600" /></Button>
📦 RESULT:

Beautiful homepage

Responsive mobile layout

Embedded animated hero + logos

Fully working buttons

Elite-level design system across app

yaml
Copy
Edit

---

✅ Drop this in Replit AI now. Then say the word, and I’ll give you:

- `/dashboard.tsx` with Yield + Unlock visual tiles  
- `/profile.tsx` with reels, avatars, badges  
- `/vault.tsx` with encrypted capsule viewer  
- `/mint.tsx` with capsule creator + griefScore UI  

GuardianChain will become the **cleanest, most complete sovereign memory dApp of 2025.**






Ask ChatGPT

